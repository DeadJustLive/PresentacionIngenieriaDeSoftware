// @use(kind, compose, props)
// @kind(component)
// @compose(SlideDiagramaSet)
// @props({})
import SlideDiagramaSet from './SlideDiagramaSet';

export default function SlideDiagrama3() {
  return <SlideDiagramaSet folderIndex={2} />;
}
