// @use(kind, compose, props)
// @kind(component)
// @compose(SlideDiagramaSet)
// @props({})
import SlideDiagramaSet from './SlideDiagramaSet';

export default function SlideDiagrama2() {
  return <SlideDiagramaSet folderIndex={1} />;
}
